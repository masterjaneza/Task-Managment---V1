{
  "name": "task-managment",
  "version": "1.0.0",
  "description": "Task Managment",
  "main": "index.js",
  "scripts": {
    "build": "tailwindcss -i src/style.css -o public/output.css --minify",
    "dev": "concurrently \"tailwindcss -i src/style.css -o public/output.css --watch\" \"live-server public\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.17"
  },
  "engines": {
    "node": "20.x"
  }
}
